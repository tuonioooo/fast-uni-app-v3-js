# jasper-login 优化建议清单

> 基于 `uni_modules/jasper-login/优化建议.md` 整理。
> 状态说明：
> - `[x]` 已完成
> - `[ ]` 未完成
> - `[-]` 暂不处理 / 非当前阶段重点

## P0 必做

### 1. 编码与乱码

- [x] `readme.md` 已恢复为可正常阅读的 UTF-8 内容
- [x] `index.js` 已确认是正常 UTF-8 内容
- [x] `config.js` 已确认是正常 UTF-8 内容
- [x] `common/login-api.js` 已确认是正常 UTF-8 内容
- [x] `services/auth.service.js` / `services/auth-service.js` 已确认是正常 UTF-8 内容
- [x] `components/b-fab-login/b-fab-login.vue` 文案与注释已确认正常
- [x] `pages/login/login-withoutpwd.vue` 文案与注释已确认正常
- [x] `common/login-page.scss` 已确认正常

### 2. 空壳文件与模块出口

- [x] 已新增 `components/jasper-login/jasper-login.vue` 作为统一对外组件入口
- [x] 模块统一对外入口已通过 `index.js` 导出并注册组件
- [x] `pages/login/login-withpwd.vue` 已补齐为可用的账号密码登录页

### 3. 事件总线清理

- [x] 主链路已移除事件总线常量依赖，`constants/events.js` 已删除
- [x] `login-withoutpwd.vue` 中 `onLoad` 注册的事件已在 `onUnload` 对应解绑
- [x] 伪生命周期 `unLoad()` 风险点目前未再看到作为主要解绑入口
- [x] 登录主链路已移除对 `uni.$on / uni.$emit / uni.$off` 的运行时依赖
- [x] `b-fab-login -> login-withoutpwd` 已改成显式组件事件
- [x] `login-withoutpwd -> login-smscode` 已改成显式路由参数传递
- [x] `login-smscode` 已改成页面内自管理发码、倒计时和登录处理

### 4. 示例残留与硬编码

- [x] 主流程里未再发现 `16270569395` 这类前端演示手机号硬编码
- [x] 直接依赖 `@/api` 的实现已去除
- [x] 缺失的 `weixin-mixin.js` 已不再被引用
- [x] `common/sampleApi.js` 已迁移为 `examples/sample-auth-api.js`
- [ ] `services/auth-service.js` 目前仍是 mock 风格实现，不是生产适配层

### 5. 缺失依赖说明

- [x] 模块已新增 `services/auth.service.js`，并补充统一命名入口 `services/auth-service.js`
- [x] `readme.md` 已补充宿主项目接口、全局对象、接入边界说明
- [x] 模块与 `uni.$jasper` 的依赖已通过 `utils/runtime-deps.js` 显式收口

## P1 建议尽快处理

### 6. 抽离服务层与适配层

- [x] 已增加 `services/auth.service.js` / `services/auth-service.js`
- [x] 已增加 `common/login-api.js` 作为统一调用出口
- [x] 已支持 adapter / injectable service 模式，新增 `services/service-registry.js`
- [-] `services/sms.js`、`services/weixin.js` 暂不拆分，当前阶段继续由 `auth-service.js` 承载
- [x] 已支持宿主通过安装配置注入 API 实现

### 7. 登录成功后的持久化解耦

- [x] 已抽出 `onLoginSuccess`
- [x] 已抽出 `persistToken`
- [x] 已抽出 `persistUserInfo`
- [x] 登录成功后的存储与状态写入已支持通过 hooks 解耦

### 8. 配置归一化

- [x] `loginTypes` 已通过 `normalizeLoginConfig` 做去重
- [x] 已形成 `base config / platform overrides / runtime normalization` 三层结构
- [x] 已补充配置校验与边界归一化处理

### 9. 页面复杂度拆分

- [x] `pages/login/login-withoutpwd.vue` 已拆出核心流程辅助逻辑
- [x] 已拆出 `useLoginFlow`
- [x] 已拆出 `useSmsLogin`
- [x] 已拆出 `useThirdPartyLogin`

## P2 中期优化

### 10. 功能补齐

- [x] 补齐账号密码登录页 `login-withpwd.vue`
- [x] 已明确账号密码登录接口协议
- [x] 已补齐统一组件入口 `components/jasper-login/jasper-login.vue`

### 11. 类型与注释

- [x] JSDoc 已补充并覆盖关键接口说明
- [x] 已补充更稳定的接口类型约束
- [x] 已新增 `types/jasper-login.d.ts`

### 12. 示例与接入文档

- [x] 已补充最小可运行示例
- [x] 已补充接入前置条件清单
- [x] 已补充各平台差异说明
- [x] 已明确区分哪些能力可直接用于生产、哪些只是示例

## 结构性重构项

- [x] 已落成 `composables/` -> 现在已更名为 `hooks`
- [-] 未按建议落成 `adapters/`
- [x] 已落成 `config/base.js`
- [x] 已新增统一命名入口并完成内部切换：组件统一为 `login-*`，服务统一为 `auth-service.js`，页面实例名统一为语义化命名；旧文件保留兼容转发

## 当前结论

- [x] P0 主链路整改已基本完成，编码、事件、页面主流程、配置归一化、文档补充已收口
- [x] P1 里的服务注册、登录成功解耦、页面复杂度拆分已基本完成
- [x] 结构性命名收敛已完成，当前主入口已统一为 `login-*` 组件与 `auth-service.js`
- [ ] 当前最主要的剩余工作是把 `services/auth-service.js` 从 mock 演示实现替换为真实生产适配层
- [-] `adapters/` 目录落地、`sms.js / weixin.js` 进一步拆分属于后续结构优化项，不影响当前主链路可用性
