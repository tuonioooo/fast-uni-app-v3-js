# jasper-login 优化建议清单

> 基于原“脚手架审查与优化建议”并结合当前代码实现复核整理。  
> 复核时间：2026-04-25
>
> 状态说明：
> - `[x]` 已完成
> - `[ ]` 未完成
> - `[-]` 暂不处理 / 属于后续结构优化

## 本次复核结论

当前主链路已经基本收口，真正还没完成的核心项已经不多：

- [ ] `services/auth-service.js` 仍然是 mock 演示实现，尚未替换为真实生产适配层
- [-] `adapters/` 目录尚未落地，`sms / weixin / auth` 也没有进一步拆分为独立适配文件
- [x] 文档中的旧文件名、旧组件名已需要同步到当前实现，本清单已按现状修正

换句话说，当前不是“还有很多没做”，而是“大部分已经做完，剩下 1 个核心接入项 + 1 组可选结构优化项”。

## P0 必做

### 1. 编码与乱码

- [x] `readme.md` 已恢复为可正常阅读的 UTF-8 内容
- [x] `index.js` 已确认是正常 UTF-8 内容
- [x] `config.js` 已确认是正常 UTF-8 内容
- [x] `common/login-api.js` 已确认是正常 UTF-8 内容
- [x] `services/auth-service.js` 已确认是正常 UTF-8 内容
- [x] `components/login-methods/login-methods.vue` 文案与注释已确认正常
- [x] `pages/login/login-withoutpwd.vue` 文案与注释已确认正常
- [x] `common/login-page.scss` 已确认正常

### 2. 空壳文件与模块出口

- [x] 已新增 `components/jasper-login/jasper-login.vue` 作为统一对外组件入口
- [x] 模块统一对外入口已通过 `index.js` 导出并注册组件
- [x] `pages/login/login-withpwd.vue` 已补齐为可用的账号密码登录页

### 3. 事件总线清理

- [x] 主链路已移除事件总线常量依赖，`constants/events.js` 已删除
- [x] 登录主链路已移除对 `uni.$on / uni.$emit / uni.$off` 的运行时依赖
- [x] `login-methods -> login-withoutpwd` 已改成显式组件事件
- [x] `login-withoutpwd -> login-smscode` 已改成显式路由参数传递
- [x] `login-smscode` 已改成页面内自管理发码、倒计时和登录处理
- [x] `login-withoutpwd.vue` 中原先页面级副作用已在 `onUnload` 做对应清理

### 4. 示例残留与硬编码

- [x] 主流程里未再发现 `16270569395` 这类前端演示手机号硬编码
- [x] 直接依赖 `@/api` 的实现已去除
- [x] 缺失的 `weixin-mixin.js` 已不再被引用
- [x] `common/sampleApi.js` 已迁移为 `docs/examples/sample-auth-api.js`
- [ ] `services/auth-service.js` 目前仍是 mock 风格实现，不是生产适配层

### 5. 缺失依赖说明

- [x] `readme.md` 已补充宿主项目接口、全局对象、接入边界说明
- [x] 模块与 `uni.$jasper` 的依赖已通过 `utils/runtime-deps.js` 显式收口
- [x] 页面层已统一通过 `common/login-api.js` 访问服务，而不是直接散落依赖具体实现

## P1 建议尽快处理

### 6. 服务层与适配层

- [x] 已形成 `common/login-api.js -> service-registry.js -> auth-service.js` 的统一调用链
- [x] 已支持宿主通过安装配置注入 `services`
- [x] 已支持宿主通过安装配置注入登录成功 hooks
- [ ] 默认导出的 `auth-service.js` 仍需由宿主替换为真实请求实现，才能算完成生产接入
- [-] `services/sms.js`、`services/weixin.js`、`services/auth.js` 暂未拆分，当前阶段继续由 `auth-service.js` 承载
- [-] `adapters/` 目录暂未落地；当前已有“可注入服务注册中心”，所以不是主链路阻塞项

### 7. 登录成功后的持久化解耦

- [x] 已抽出 `persistToken`
- [x] 已抽出 `persistUserInfo`
- [x] 已抽出 `onLoginSuccess`
- [x] 默认实现已通过 `services/service-registry.js` 统一注册
- [x] 宿主可按需覆盖默认落库逻辑

### 8. 配置归一化

- [x] `loginTypes` 已通过 `normalizeLoginConfig` 做去重与过滤
- [x] 已形成 `base config / platform overrides / runtime normalization` 三层结构
- [x] 已补充路径、协议配置等边界归一化处理

### 9. 页面复杂度拆分

- [x] `pages/login/login-withoutpwd.vue` 已拆出核心流程辅助逻辑
- [x] 已拆出 `useLoginFlow`
- [x] 已拆出 `useSmsLogin`
- [x] 已拆出 `useThirdPartyLogin`

## P2 中期优化

### 10. 功能补齐

- [x] 已补齐账号密码登录页 `login-withpwd.vue`
- [x] 已明确账号密码登录接口协议
- [x] 已补齐统一组件入口 `components/jasper-login/jasper-login.vue`

### 11. 类型与注释

- [x] JSDoc 已补充并覆盖关键接口说明
- [x] 已补充更稳定的接口类型约束
- [x] 已新增 `types/jasper-login.d.ts`

### 12. 示例与接入文档

- [x] 已补充最小可运行示例
- [x] 已补充接入前置条件清单
- [x] 已补充各平台差异说明
- [x] 已明确区分哪些能力可直接用于生产、哪些只是示例

## 结构性重构项

- [x] 原建议中的 `composables/` 已以 `hooks/` 目录形式落地
- [x] 已落成 `config/base.js`
- [x] 已完成组件、页面、服务主入口的统一命名收敛
- [-] 未按建议单独落成 `adapters/`
- [-] 未继续把服务拆到更细粒度文件，但这不影响当前主链路可用性

## 当前还差哪些内容

### 必须补齐

- [ ] 把 `services/auth-service.js` 从 mock 演示实现替换为真实生产适配层
  - 需要至少落地真实的 `sendSms`
  - 需要至少落地真实的 `verifySms`
  - 需要至少落地真实的 `loginByPhone`
  - 需要至少落地真实的 `loginByPassword`
  - 如使用微信手机号授权，还需要落地真实的 `getPhoneInfoByCode`

### 可以继续优化，但不阻塞当前使用

- [-] 新建 `adapters/` 目录，把第三方接入按能力拆分
- [-] 将 `auth-service.js` 按 `sms / auth / weixin` 继续拆成更清晰的独立模块
- [-] 在文档中继续收敛旧命名痕迹，避免后续再出现历史名和现名并存的情况

## 当前结论

- [x] P0 主链路整改已基本完成，编码、事件、页面主流程、配置归一化、文档补充已收口
- [x] P1 中的服务注册、登录成功解耦、页面复杂度拆分已完成
- [x] 当前对外接入方式已经稳定：页面走 `common/login-api.js`，服务走 `service-registry.js`，宿主可通过 `app.use(JasperLogin, options)` 注入实现
- [ ] 当前最主要的剩余工作只有一项：把 `services/auth-service.js` 从 mock 实现替换为真实生产实现
- [-] `adapters/` 目录落地与更细粒度服务拆分属于后续结构优化项，不影响当前脚手架主链路可用性
